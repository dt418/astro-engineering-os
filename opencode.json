{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "astro-orchestrator": {
      "type": "local",
      "command": ["node", "./orchestrator/dist/mcp/server.js"],
      "enabled": false,
      "disabledReason": "stdio transport pending — use createMcpServer() in-process until transport is added"
    }
  }
}
